@model List<WebApplication1.Models.StudentsDashboard>
@{
    ViewBag.Title = "Students";
    Layout = "~/Views/Shared/loggedLayout.cshtml";
}

@*
    @if (ViewBag.Deleted != null && (bool)ViewBag.Deleted == true)
    {
        <div class="alert alert-success">
            The record was deleted successfully.
        </div>
    }
    else if (ViewBag.Deleted != null && (bool)ViewBag.Deleted == false)
    {

            There was an error deleting the record.
        </div>
    } *@

@if (ViewBag.Id != null)
{

    if (@ViewBag.result == 0)
    {
        <div class="alert alert-danger">
            <p>Id - @ViewBag.Id Failed to Delete</p>
        </div>

    }
    else
    {
        <div class="alert alert-danger">
            <p>Id - @ViewBag.Id deleted Successfully</p>
        </div>
    }

}
@if (TempData["id"] != null && TempData["result"] != null)
{
    <div class="alert alert-info" role="alert">
        <span>id -(@TempData["id"]) : @TempData["result"]</span>
    </div>
}
<h2>Students from DATABASE by model</h2>
<table class="table table-bordered table-hover">
    <tr class="table-warning">
        <td>Name</td>
        <td>Email</td>
        <td>Gender</td>
        <td>Action</td>
    </tr>

    @foreach (var items in Model)
    {
        <tr>
            @* <td>@items.Id</td> *@
            <td><a href="@Url.Action("Details", "Dashboard", new { Id = items.Id })">@items.Name</a></td>
            <td>@items.Email</td>
            <td>@items.Gender</td>
            <td><a href="@Url.Action("Delete", "Dashboard", new { id = items.Id })" class="btn btn-danger"><i class="fas fa-trash"></i>  Delete</a></td>
        </tr>
    }

</table>
